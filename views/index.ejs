<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8"/>
    <title>Lista de Produtos</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f2f2f2;
        }
        
        h1 {
            text-align: center;
            margin: 20px 0;
        }
        
        h2 {
            margin-top: 20px;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        
        table, th, td {
            border: 1px solid #ccc;
        }
        
        th, td {
            padding: 10px;
            text-align: left;
        }
        
        thead {
            background-color: #f9f9f9;
        }
        
        /*seleciona as linhas pares da tabela*/
        tr:nth-child(even) {
            background-color: #e9e9e9;
        }
        
        a {
            text-decoration: none;
        }
        
        button {
            padding: 10px 20px;
            font-size: 14px;
            border: none;
            background-color: #e94081;
            color: white;
            cursor: pointer;
            margin-top: 10px;
            
        }
        
        button:hover {
            background-color: #e7bee2;
            color: black;
            transition: 0.5s;
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Diretório raiz</h1>
        <h2>Lista de Produtos com autor</h2>
        <table>
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Nome</th>
                    <th>Preço</th>
                    <th>Categoria</th>
                    <th>Descrição</th>
                    <th>Autor</th>
                </tr>
            </thead>
            <tbody>
                <% for (let i = 0; i < products.length; i++) { %>
                    <tr>
                        <td><%= products[i]._id %></td>
                        <td><%= products[i].name %></td>
                        <td><%= products[i].price %></td>
                        <td><%= products[i].categoria %></td>
                        <td><%= products[i].descricao %></td>
                        <td><%= products[i].createdBy.username %></td>
                    </tr> 
                <% } %> 
            </tbody>
        </table>
        <a href="/prod/my-products"><button>Produtos</button></a><br>
        <a href="/prod/new"><button>Cadastrar produto</button></a> 
    </div>
</body>
</html>
